Build 1.2.2026 — foi a build em que o sistema ganhou vida própria entre o bootloader e a tela: o kernel passou a existir como arquivo separado e a ser encontrado por varredura de assinatura mascarada em qualquer disco (CD, USB gravada com dd, Ventoy) — sem caminho fixo, sem depender de onde a mídia foi montada; quem encontrava continuava sendo o bootloader, mas o trabalho sujo (ler setor por setor, janelas decrescentes, media IDs) já morava nessa etapa. A pilha foi blindada com as regras que valem até hoje (watchdog desligado, regra dos dois desreferenciamentos antes de chamar qualquer função do firmware) e o kernel passou a escrever na tela e ficar lá
